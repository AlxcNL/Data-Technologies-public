# Physical Design – Creating Tables in SQL


## Data Types
Understanding Data Types (VARCHAR, TEXT, INT, BOOLEAN, DATE, JSONB).

## Creating tables using SQL syntax


## Constraints & Data Integrity

Primary Key (PK) vs. Foreign Key (FK)

Unique constraints (UNIQUE) for enforcing distinct values.

Check constraints (CHECK) to validate data conditions.

Cascade behavior (ON DELETE CASCADE) to manage relationships.